import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


// Rendering the app to the virtual DOM

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root'),
);
